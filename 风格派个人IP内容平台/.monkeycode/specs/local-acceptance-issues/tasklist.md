# 需求实施计划

- [x] 1. 修复选题卡片移动端布局
  - 修改 `src/styles.css` 的选题卡片移动断点规则，使正文、评分证据和操作区上下排列。
  - 保证 390px 下正文可读、按钮可操作且无横向溢出。
  - [x] 1.1 增加 390px 布局回归检查，验证正文宽度和操作区排列。

- [x] 2. 修复选题生成错误反馈
  - 检查 `apiJson` 的非 2xx 错误解析，保留服务端 `error`、`missing_fields` 和状态码。
  - 修改 `WorkspaceView.generateTopics`，处理 422、网络错误、5xx 和成功清理状态。
  - 将缺失字段映射为中文并通过 `role="alert"` 展示。
  - [x] 2.1 增加选题生成错误路径测试，覆盖无来源、档案不足和通用失败提示。

- [x] 3. 修复撤回原因展示
  - 修改 `DraftApprovalPanel` 的撤回状态优先级。
  - 始终展示实际撤回原因；缺失原因时显示“需要修改”。
  - 将重新检查提示独立展示，避免被门禁提示覆盖。
  - [x] 3.1 增加撤回原因存在、缺失和重复撤回的展示回归检查。

- [x] 4. 统一发布前检查状态文案
  - 在 `src/main.tsx` 增加展示层状态映射。
  - 将 `passed`、`warning`、`blocked` 映射为“已通过”“有提醒”“未通过”。
  - 保留原始状态值用于判断和 API 交互，不修改服务端数据。
  - [x] 4.1 增加状态映射覆盖检查，确保页面不显示英文原始状态。

- [x] 5. 检查点
  - 运行 `npm test`、`npm run typecheck`、`npm run build`。
  - 修复所有由本次变更导致的回归。

- [x] 6. 本地浏览器验证
  - 使用 Chrome/CDP 验证 390px 布局、422 错误反馈、中文状态和撤回原因。
  - 记录验证结果，不连接生产环境，不修改产品数据。
