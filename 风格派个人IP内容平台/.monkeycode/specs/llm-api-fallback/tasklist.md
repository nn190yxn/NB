# 需求实施计划

- [x] 1. 扩展前端 API 配置模型与兼容迁移
  - 修改 `src/main.tsx` 的 `ApiSettings` 类型，增加 `llm.primary` 与 `llm.fallback`
  - 将旧版 `llm.base_url/api_key/model` 自动迁移为 `llm.primary`
  - 保证配置仍只保存到浏览器 `localStorage`

- [x] 2. 改造设置页为双 API 录入界面
  - 将现有大模型配置卡拆分为“API 1（主用）”和“API 2（备用）”
  - 两组配置均支持 Base URL、API Key、模型名和连接测试
  - 分别展示两个 API 的测试状态
  - 保持备用 API 未配置时 API 1 可独立使用
  - [x] 2.1 补充设置页前端特征测试

- [x] 3. 实现服务端统一主备调用层
  - 在 `server/index.mjs` 增加 OpenAI 兼容调用适配器
  - 实现 API 1 优先、API 1 失败后调用 API 2 的逻辑
  - 覆盖网络错误、超时、HTTP 错误、空响应和无效响应
  - 两个 API 都未配置时保留现有规则引擎
  - 禁止记录 API Key 和完整请求内容

- [x] 4. 接入现有大模型业务能力
  - 选题生成接入统一主备调用层
  - 草稿生成接入统一主备调用层
  - AI 记忆自动提炼接入统一主备调用层
  - 保留现有接口返回结构、来源引用和数据兼容
  - [x] 4.1 编写 API 1 成功时不调用 API 2 的集成测试
  - [x] 4.2 编写 API 1 失败后 API 2 接管的集成测试
  - [x] 4.3 编写双 API 失败与未配置时的回退测试

- [x] 5. 扩展连接测试接口
  - 修改 `/api/settings/test` 支持分别测试主用和备用 API
  - 统一处理 URL、鉴权、超时和响应格式错误
  - 确保测试结果不泄露密钥

- [x] 6. 验证与交付检查
  - 执行现有测试、类型检查和生产构建
  - 检查旧配置迁移、前端请求头和错误提示
  - 确认没有写入服务端数据库或日志
  - 确认生产部署文件只包含必要的前端与服务端改动
