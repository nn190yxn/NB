# 需求实施计划：内容决策与创作质量链

- [x] 1. 建立能力契约与任务上下文
  - [x] 1.1 在 `src/shared/types.ts` 增加能力标识、流程状态、检查结果、人工确认和来源引用类型；对应设计文档第 5、6 节。
  - [x] 1.2 新建能力登记模块，登记 `topic_evaluate`、`hook_generate`、`platform_adapt`、`persona_check`、`quality_gate`、`publish_checklist`；对应设计文档第 5 节。
  - [x] 1.3 新建任务上下文组装模块，只读取当前 `owner_id`、选题、素材、研究、方法、记忆和历史表现；对应设计文档第 6 节。
  - [x] 1.4 保证 API Key、无关账号数据和未授权素材不进入模型上下文；对应设计文档第 4、6、12 节。
  - [x] 1.5 为能力登记和上下文组装编写单元测试、属性测试、账号隔离测试和来源引用测试；对应设计文档第 14 节。

- [x] 2. 实现选题七维评估流程
  - [x] 2.1 为 `topics` 增加评估结果、决策、证据、建议和流程状态字段，并补充旧 JSON/MySQL 数据迁移默认值；对应设计文档第 7.1、9.1 节。
  - [x] 2.2 实现 `POST /api/topics/:id/evaluate`，读取 IP 档案、研究、素材、方法和表现数据；对应设计文档第 7.1、10 节。
  - [x] 2.3 实现流量潜力、账号匹配、竞争差异化、时效价值、变现空间、制作成本、合规风险七维评分，并返回综合分和证据；对应设计文档第 7.1 节。
  - [x] 2.4 实现 `PUT /api/topics/:id/decision`，只允许用户确认后进入创作；对应设计文档第 7.1、8.1 节。
  - [x] 2.5 保留暂缓选题及理由，不删除原始选题；对应设计文档第 7.1、8.1 节。
  - [x] 2.6 在选题助手中展示综合分、七维评分、评分证据和下一步建议；对应设计文档第 7.1、15 节。
  - [x] 2.7 编写评分边界、缺少上下文、重复提交、账号隔离和非法状态流转测试；对应设计文档第 12、14 节。

- [x] 3. 实现 Hook、草稿和平台版本流程
  - [x] 3.1 为草稿增加 Hook 候选、选中 Hook、平台版本和流程状态字段，并保留旧草稿兼容性；对应设计文档第 7.2、9.2 节。
  - [x] 3.2 实现 `POST /api/drafts/:id/hooks`，生成多个 Hook 候选并执行长度、格式和禁用词校验；对应设计文档第 5、7.2、10 节。
  - [x] 3.3 将选中的 Hook 传入现有 `/api/drafts/generate` 流程，并保留生成上下文；对应设计文档第 7.2 节。
  - [x] 3.4 保证主稿与小红书、抖音、视频号、公众号等平台版本独立保存，平台版本不得覆盖主稿；对应设计文档第 7.2、9.2 节。
  - [x] 3.5 保留选题、研究、素材、方法和 Hook 的 `source_refs`；对应设计文档第 4、7.2、9 节。
  - [x] 3.6 在内容创作页增加 Hook 选择、主稿/平台版本切换和版本状态展示；对应设计文档第 7.2、15 节。
  - [x] 3.7 编写 Hook 校验、平台版本独立性、草稿版本恢复和来源链完整性测试；对应设计文档第 12、14 节。

- [x] 4. 实现人设、质量和发布完整性三道检查
  - [x] 4.1 新建 `server/draft-checks.mjs`，定义统一检查结果、版本绑定、风险词和档案匹配的纯函数边界；对应 Task 4 设计第 2、3、8 节。
  - [x] 4.2 实现 `POST /api/drafts/:id/checks/persona`，对角色、受众、内容支柱、表达偏好和禁用表达做确定性检查；仅返回 `passed/warning` 并进入 `persona_checked`；对应 Task 4 设计第 4.1 节。
  - [x] 4.3 实现 `POST /api/drafts/:id/checks/quality`，检查 Hook、受众问题、核心观点、来源证据、正文结构和风险表达，输出五维 0–100 分结果；对应 Task 4 设计第 4.2 节。
  - [x] 4.4 实现质量门前置状态、综合分阈值和阻断状态流转；阻断进入 `needs_revision`，通过进入 `quality_checked`，并保存版本、证据和建议；对应 Task 4 设计第 4.2、5 节。
  - [x] 4.5 实现 `POST /api/drafts/:id/checks/publish`，检查标题、正文、平台、来源、事实核验、有效 Hook、风险词和版本组；通过进入 `publish_ready`，失败进入 `needs_revision`；对应 Task 4 设计第 4.3、5 节。
  - [x] 4.6 在 `PUT /api/drafts/:id` 修改标题、正文、平台、来源或 Hook 后回到 `content_ready`，清除下游当前结果；三个检查接口支持同版本幂等和 `force=true` 重试；明确阻断时禁止进入 `ready_to_shoot`；对应 Task 4 设计第 3、5、6 节。
  - [x] 4.7 在 `src/main.tsx` 和 `src/styles.css` 增加人设→质量→发布顺序操作、状态/分数/证据/建议展示和重试入口，不新增页面结构；对应 Task 4 设计第 7 节。
  - [x] 4.8 编写 `server/draft-checks.test.mjs`、`server/draft-checks-api.test.mjs`，覆盖三类结果、顺序约束、阻断回退、修改重试、幂等、来源保留、平台独立性、旧草稿兼容和账号隔离；对应 Task 4 设计第 9 节。

- [x] 5. 实现人工确认与今日拍摄衔接
  - [x] 5.1 强化草稿状态机：只有质量门和发布清单通过后，才能进入 `ready_to_shoot`；对应设计文档第 8.2 节。
  - [x] 5.2 增加草稿人工确认记录，保存确认时间、用户和确认前检查结果；对应设计文档第 7.2、8.2 节。
  - [x] 5.3 复用现有 `PUT /api/shooting/:id`，将确认后的草稿安全加入今日拍摄；对应设计文档第 7.2、10 节。
  - [x] 5.4 阻止未确认草稿、质量阻断草稿和缺少事实核验的草稿进入拍摄或发布状态；对应设计文档第 4、7.2、8.2 节。
  - [x] 5.5 在内容创作页增加“确认进入拍摄”操作和状态回退；对应设计文档第 7.2、15 节。
  - [x] 5.6 编写从选题确认到今日拍摄的端到端流程测试；对应设计文档第 14、15 节。

- [x] 5. 实现人工确认与今日拍摄衔接
  - [x] 5.1 扩展 `server/draft-workflow.mjs` 的 `approval` 默认字段，兼容旧草稿并保留已有确认记录；对应设计第 4 节。
  - [x] 5.2 在 `server/index.mjs` 增加 `POST /api/drafts/:id/approve`，校验当前版本、人设检查、质量门、发布清单、事实核验和账号归属；对应设计第 5.1、6 节。
  - [x] 5.3 在确认接口中原子保存确认快照、更新草稿状态，并按 `owner_id + draft_id` 幂等创建今日拍摄项；保留正文、平台、策略字段和 `source_refs`；对应设计第 5.2 节。
  - [x] 5.4 增加 `POST /api/drafts/:id/revoke-approval`，支持未发布草稿回退、撤回原因和拍摄项状态同步；禁止已发布内容撤回；对应设计第 5.3、6 节。
  - [x] 5.5 收紧草稿状态写入，禁止客户端通过 `approval`、`workflow_status` 或检查快照绕过确认；更新既有直接进入 `ready_to_shoot` 的测试路径；对应设计第 5、8 节。
  - [x] 5.6 在 `src/main.tsx` 和 `src/styles.css` 增加“确认进入拍摄”“撤回确认”、确认人/时间/状态和今日拍摄入口；不新增页面；对应设计第 7 节。
  - [x] 5.7 编写 `server/draft-approval.test.mjs`，覆盖默认字段、确认快照不可变、前置校验、状态保护和来源保留；对应设计第 9 节。
  - [x] 5.8 编写 `server/draft-approval-api.test.mjs`，覆盖确认、幂等创建、撤回、版本冲突、账号隔离、已发布保护和旧数据兼容；对应设计第 9 节。
  - [x] 5.9 编写选题确认 → 草稿检查 → 人工确认 → 今日拍摄的端到端流程测试；对应设计第 9、10 节。
  - [x] 5.10 检查点：专项测试、全量测试、类型检查和生产构建通过。

- [x] 6. 完成第一阶段质量门审计
  - [x] 6.1 扩展旧 JSON 选题、草稿、确认和拍摄记录兼容测试，验证安全默认值、已有字段保留和归一化幂等；对应 Task 6 设计第 3.1、6 节。
  - [x] 6.2 扩展 MySQL 集合往返测试，验证 `evaluation`、Hook/版本字段、`checks`、`approval.checks_snapshot` 和 `source_refs` 不丢失；对应 Task 6 设计第 3.1、6 节。
  - [x] 6.3 扩展任务 1–5 新增接口的 `owner_id` 隔离和越权隐藏测试；对应 Task 6 设计第 3.2、6 节。
  - [x] 6.4 扩展选题→草稿→检查→确认→拍摄链路的 `source_refs` 完整性测试；对应 Task 6 设计第 3.2、4、6 节。
  - [x] 6.5 使用合成哨兵值检查内容上下文、公开响应和生产构建产物，确认 API Key、密码、Token 和私钥不泄露；对应 Task 6 设计第 3.2、4、6 节。
  - [x] 6.6 扩展检查顺序、质量阻断、内容修改失效、`force=true` 重试和旧结果不可复用测试；对应 Task 6 设计第 3.3、6 节。
  - [x] 6.7 扩展版本冲突、确认/撤回幂等、拍摄发布保护和已发布不可撤回测试；对应 Task 6 设计第 3.3、6 节。
  - [x] 6.8 仅在审计测试发现真实缺陷时实施与验收直接相关的最小修复，不新增运行时模块或无关重构；对应 Task 6 设计第 1、5、8 节。
  - [x] 6.9 运行专项测试、`npm test`、`npm run typecheck`、`npm run build` 和 `npm run verify`；对应 Task 6 设计第 3.4、7 节。
  - [x] 6.10 扫描代码和依赖范围，确认没有浏览器自动连接、自动发布或全量 Easel Skill 移植；对应 Task 6 设计第 2、7 节。
  - [x] 6.11 更新任务清单、README、架构、接口、开发指南、任务登记和任务记忆；对应 Task 6 设计第 7 节。
  - [x] 6.12 检查点：确保所有测试通过，如有疑问请询问用户。
