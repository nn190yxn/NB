# IP 档案双区实施计划

- [x] 1. 扩展 IP 档案页面数据读取与分区展示
  - 在 `src/main.tsx` 的 `ProfileReviewPanel` 同时读取 `/api/profile` 与 `/api/positioning`。
  - 新增“已确认基础档案”区域，展示定位、受众、问题、支柱、观点、语气、边界、目标和来源。
  - 待审核区域仅渲染 `status === 'pending'` 的记录；无记录时保留空状态。
  - 对应设计：页面结构、数据流、兼容与边界。

- [x] 2. 保持审核接口增量合并并补充状态保护
  - 检查现有 `/api/profile/reviews/:id` 审核通过逻辑，确保只合并审核字段，不覆盖未提交字段。
  - 必要时补充服务端过滤或前端状态过滤，保证 confirmed/rejected 不进入待审核区。
  - 对应设计：数据流、兼容与边界。

- [x] 3. 补充回归测试与样式适配
  - 在 `server/web-assets.test.mjs` 增加基础档案双区和 pending 过滤的前端特征断言。
  - 检查双区在桌面和移动布局下不破坏现有页面结构。
  - 对应设计：验收标准。

- [x] 4. 检查点：确保所有测试通过
  - 运行 `npm run verify`。
  - 检查 diff，确认未改动研究、素材、选题和草稿数据。
